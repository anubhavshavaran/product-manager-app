export type Product = {
    id: Number,
    slug: string,
    productName: string,
    brand: string,
    type: string,
    warrantyPeriod: Number,
    startDate: string,
    endDate: string,
    price: Number,
    serialNumber: string,
    purchaseDate: string,
    createdAt: string
}